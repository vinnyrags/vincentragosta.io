import {
  createProperties,
  DefaultPropertyStructure,
} from "@/directives/properties";
import { gridFixture } from "@/directives/grid/fixture";

const gridProperties: DefaultPropertyStructure = createProperties(gridFixture);
export default gridProperties;
