import {
  createProperties,
  DefaultPropertyStructure,
} from "@/directives/properties";
import { borderFixture } from "@/directives/border/fixture";

const borderProperties: DefaultPropertyStructure =
  createProperties(borderFixture);
export default borderProperties;
