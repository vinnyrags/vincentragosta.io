import {
  createProperties,
  DefaultPropertyStructure,
} from "@/components/directives/properties";
import { containerFixture } from "@/components/directives/container/fixture";

const containerProperties: DefaultPropertyStructure =
  createProperties(containerFixture);
export default containerProperties;
