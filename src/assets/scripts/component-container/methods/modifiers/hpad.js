export function maybeAddHorizontalPaddingModifiers(props) {
    let modifiers = [];

    if (props.hpadNone) {
        modifiers.push('hpad-none');
    }

    if (props.hpadHalf) {
        modifiers.push('hpad-half');
    }

    if (props.hpadDefault) {
        modifiers.push('hpad-default');
    }

    if (props.hpadDouble) {
        modifiers.push('hpad-double');
    }

    if (props.hpadTriple) {
        modifiers.push('hpad-triple');
    }

    if (props.hpadQuadruple) {
        modifiers.push('hpad-quadruple');
    }

    if (props.hpadXsNone) {
        modifiers.push('hpad-xs-none');
    }

    if (props.hpadXsHalf) {
        modifiers.push('hpad-xs-half');
    }

    if (props.hpadXsDefault) {
        modifiers.push('hpad-xs-default');
    }

    if (props.hpadXsDouble) {
        modifiers.push('hpad-xs-double');
    }

    if (props.hpadXsTriple) {
        modifiers.push('hpad-xs-triple');
    }

    if (props.hpadXsQuadruple) {
        modifiers.push('hpad-xs-quadruple');
    }

    if (props.hpadSmNone) {
        modifiers.push('hpad-sm-none');
    }

    if (props.hpadSmHalf) {
        modifiers.push('hpad-sm-half');
    }

    if (props.hpadSmDefault) {
        modifiers.push('hpad-sm-default');
    }

    if (props.hpadSmDouble) {
        modifiers.push('hpad-sm-double');
    }

    if (props.hpadSmTriple) {
        modifiers.push('hpad-sm-triple');
    }

    if (props.hpadSmQuadruple) {
        modifiers.push('hpad-sm-quadruple');
    }

    if (props.hpadMdNone) {
        modifiers.push('hpad-md-none');
    }

    if (props.hpadMdHalf) {
        modifiers.push('hpad-md-half');
    }

    if (props.hpadMdDefault) {
        modifiers.push('hpad-md-default');
    }

    if (props.hpadMdDouble) {
        modifiers.push('hpad-md-double');
    }

    if (props.hpadMdTriple) {
        modifiers.push('hpad-md-triple');
    }

    if (props.hpadMdQuadruple) {
        modifiers.push('hpad-md-quadruple');
    }

    if (props.hpadLgNone) {
        modifiers.push('hpad-lg-none');
    }

    if (props.hpadLgHalf) {
        modifiers.push('hpad-lg-half');
    }

    if (props.hpadLgDefault) {
        modifiers.push('hpad-lg-default');
    }

    if (props.hpadLgDouble) {
        modifiers.push('hpad-lg-double');
    }

    if (props.hpadLgTriple) {
        modifiers.push('hpad-lg-triple');
    }

    if (props.hpadLgQuadruple) {
        modifiers.push('hpad-lg-quadruple');
    }

    if (props.hpadXlNone) {
        modifiers.push('hpad-xl-none');
    }

    if (props.hpadXlHalf) {
        modifiers.push('hpad-xl-half');
    }

    if (props.hpadXlDefault) {
        modifiers.push('hpad-xl-default');
    }

    if (props.hpadXlDouble) {
        modifiers.push('hpad-xl-double');
    }

    if (props.hpadXlTriple) {
        modifiers.push('hpad-xl-triple');
    }

    if (props.hpadXlQuadruple) {
        modifiers.push('hpad-xl-quadruple');
    }

    if (props.lpadNone) {
        modifiers.push('lpad-none');
    }

    if (props.lpadHalf) {
        modifiers.push('lpad-half');
    }

    if (props.lpadDefault) {
        modifiers.push('lpad-default');
    }

    if (props.lpadDouble) {
        modifiers.push('lpad-double');
    }

    if (props.lpadTriple) {
        modifiers.push('lpad-triple');
    }

    if (props.lpadQuadruple) {
        modifiers.push('lpad-quadruple');
    }

    if (props.lpadXsNone) {
        modifiers.push('lpad-xs-none');
    }

    if (props.lpadXsHalf) {
        modifiers.push('lpad-xs-half');
    }

    if (props.lpadXsDefault) {
        modifiers.push('lpad-xs-default');
    }

    if (props.lpadXsDouble) {
        modifiers.push('lpad-xs-double');
    }

    if (props.lpadXsTriple) {
        modifiers.push('lpad-xs-triple');
    }

    if (props.lpadXsQuadruple) {
        modifiers.push('lpad-xs-quadruple');
    }

    if (props.lpadSmNone) {
        modifiers.push('lpad-sm-none');
    }

    if (props.lpadSmHalf) {
        modifiers.push('lpad-sm-half');
    }

    if (props.lpadSmDefault) {
        modifiers.push('lpad-sm-default');
    }

    if (props.lpadSmDouble) {
        modifiers.push('lpad-sm-double');
    }

    if (props.lpadSmTriple) {
        modifiers.push('lpad-sm-triple');
    }

    if (props.lpadSmQuadruple) {
        modifiers.push('lpad-sm-quadruple');
    }

    if (props.lpadMdNone) {
        modifiers.push('lpad-md-none');
    }

    if (props.lpadMdHalf) {
        modifiers.push('lpad-md-half');
    }

    if (props.lpadMdDefault) {
        modifiers.push('lpad-md-default');
    }

    if (props.lpadMdDouble) {
        modifiers.push('lpad-md-double');
    }

    if (props.lpadMdTriple) {
        modifiers.push('lpad-md-triple');
    }

    if (props.lpadMdQuadruple) {
        modifiers.push('lpad-md-quadruple');
    }

    if (props.lpadLgNone) {
        modifiers.push('lpad-lg-none');
    }

    if (props.lpadLgHalf) {
        modifiers.push('lpad-lg-half');
    }

    if (props.lpadLgDefault) {
        modifiers.push('lpad-lg-default');
    }

    if (props.lpadLgDouble) {
        modifiers.push('lpad-lg-double');
    }

    if (props.lpadLgTriple) {
        modifiers.push('lpad-lg-triple');
    }

    if (props.lpadLgQuadruple) {
        modifiers.push('lpad-lg-quadruple');
    }

    if (props.lpadXlNone) {
        modifiers.push('lpad-xl-none');
    }

    if (props.lpadXlHalf) {
        modifiers.push('lpad-xl-half');
    }

    if (props.lpadXlDefault) {
        modifiers.push('lpad-xl-default');
    }

    if (props.lpadXlDouble) {
        modifiers.push('lpad-xl-double');
    }

    if (props.lpadXlTriple) {
        modifiers.push('lpad-xl-triple');
    }

    if (props.lpadXlQuadruple) {
        modifiers.push('lpad-xl-quadruple');
    }

    if (props.rpadNone) {
        modifiers.push('bpad-none');
    }

    if (props.bpadHalf) {
        modifiers.push('bpad-half');
    }

    if (props.rpadDefault) {
        modifiers.push('bpad-default');
    }

    if (props.rpadDouble) {
        modifiers.push('bpad-double');
    }

    if (props.rpadTriple) {
        modifiers.push('bpad-triple');
    }

    if (props.rpadQuadruple) {
        modifiers.push('bpad-quadruple');
    }

    if (props.rpadXsNone) {
        modifiers.push('bpad-xs-none');
    }

    if (props.rpadXsHalf) {
        modifiers.push('bpad-xs-half');
    }

    if (props.rpadXsDefault) {
        modifiers.push('bpad-xs-default');
    }

    if (props.rpadXsDouble) {
        modifiers.push('bpad-xs-double');
    }

    if (props.rpadXsTriple) {
        modifiers.push('bpad-xs-triple');
    }

    if (props.rpadXsQuadruple) {
        modifiers.push('bpad-xs-quadruple');
    }

    if (props.rpadSmNone) {
        modifiers.push('bpad-sm-none');
    }

    if (props.bpadSmHalf) {
        modifiers.push('bpad-sm-half');
    }

    if (props.rpadSmDefault) {
        modifiers.push('bpad-sm-default');
    }

    if (props.rpadSmDouble) {
        modifiers.push('bpad-sm-double');
    }

    if (props.rpadSmTriple) {
        modifiers.push('bpad-sm-triple');
    }

    if (props.rpadSmQuadruple) {
        modifiers.push('bpad-sm-quadruple');
    }

    if (props.rpadMdNone) {
        modifiers.push('bpad-md-none');
    }

    if (props.bpadMdHalf) {
        modifiers.push('bpad-md-half');
    }

    if (props.rpadMdDefault) {
        modifiers.push('bpad-md-default');
    }

    if (props.rpadMdDouble) {
        modifiers.push('bpad-md-double');
    }

    if (props.rpadMdTriple) {
        modifiers.push('bpad-md-triple');
    }

    if (props.rpadMdQuadruple) {
        modifiers.push('bpad-md-quadruple');
    }

    if (props.rpadLgNone) {
        modifiers.push('bpad-lg-none');
    }

    if (props.bpadLgHalf) {
        modifiers.push('bpad-lg-half');
    }

    if (props.rpadLgDefault) {
        modifiers.push('bpad-lg-default');
    }

    if (props.rpadLgDouble) {
        modifiers.push('bpad-lg-double');
    }

    if (props.rpadLgTriple) {
        modifiers.push('bpad-lg-triple');
    }

    if (props.rpadLgQuadruple) {
        modifiers.push('bpad-lg-quadruple');
    }

    if (props.rpadXlNone) {
        modifiers.push('bpad-xl-none');
    }

    if (props.bpadXlHalf) {
        modifiers.push('bpad-xl-half');
    }

    if (props.rpadXlDefault) {
        modifiers.push('bpad-xl-default');
    }

    if (props.rpadXlDouble) {
        modifiers.push('bpad-xl-double');
    }

    if (props.rpadXlTriple) {
        modifiers.push('bpad-xl-triple');
    }

    if (props.rpadXlQuadruple) {
        modifiers.push('bpad-xl-quadruple');
    }

    return modifiers;
}