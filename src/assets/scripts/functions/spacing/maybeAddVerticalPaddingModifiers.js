export function maybeAddVerticalPaddingModifiers(props) {
    let modifiers = [];

    if (props.vpadNone) {
        modifiers.push('vpad-none');
    }

    if (props.vpadHalf) {
        modifiers.push('vpad-half');
    }

    if (props.vpadDefault) {
        modifiers.push('vpad-default');
    }

    if (props.vpadDouble) {
        modifiers.push('vpad-double');
    }

    if (props.vpadTriple) {
        modifiers.push('vpad-triple');
    }

    if (props.vpadQuadruple) {
        modifiers.push('vpad-quadruple');
    }

    if (props.vpadXsNone) {
        modifiers.push('vpad-xs-none');
    }

    if (props.vpadXsHalf) {
        modifiers.push('vpad-xs-half');
    }

    if (props.vpadXsDefault) {
        modifiers.push('vpad-xs-default');
    }

    if (props.vpadXsDouble) {
        modifiers.push('vpad-xs-double');
    }

    if (props.vpadXsTriple) {
        modifiers.push('vpad-xs-triple');
    }

    if (props.vpadXsQuadruple) {
        modifiers.push('vpad-xs-quadruple');
    }

    if (props.vpadSmNone) {
        modifiers.push('vpad-sm-none');
    }

    if (props.vpadSmHalf) {
        modifiers.push('vpad-sm-half');
    }

    if (props.vpadSmDefault) {
        modifiers.push('vpad-sm-default');
    }

    if (props.vpadSmDouble) {
        modifiers.push('vpad-sm-double');
    }

    if (props.vpadSmTriple) {
        modifiers.push('vpad-sm-triple');
    }

    if (props.vpadSmQuadruple) {
        modifiers.push('vpad-sm-quadruple');
    }

    if (props.vpadMdNone) {
        modifiers.push('vpad-md-none');
    }

    if (props.vpadMdHalf) {
        modifiers.push('vpad-md-half');
    }

    if (props.vpadMdDefault) {
        modifiers.push('vpad-md-default');
    }

    if (props.vpadMdDouble) {
        modifiers.push('vpad-md-double');
    }

    if (props.vpadMdTriple) {
        modifiers.push('vpad-md-triple');
    }

    if (props.vpadMdQuadruple) {
        modifiers.push('vpad-md-quadruple');
    }

    if (props.vpadLgNone) {
        modifiers.push('vpad-lg-none');
    }

    if (props.vpadLgHalf) {
        modifiers.push('vpad-lg-half');
    }

    if (props.vpadLgDefault) {
        modifiers.push('vpad-lg-default');
    }

    if (props.vpadLgDouble) {
        modifiers.push('vpad-lg-double');
    }

    if (props.vpadLgTriple) {
        modifiers.push('vpad-lg-triple');
    }

    if (props.vpadLgQuadruple) {
        modifiers.push('vpad-lg-quadruple');
    }

    if (props.vpadXlNone) {
        modifiers.push('vpad-xl-none');
    }

    if (props.vpadXlHalf) {
        modifiers.push('vpad-xl-half');
    }

    if (props.vpadXlDefault) {
        modifiers.push('vpad-xl-default');
    }

    if (props.vpadXlDouble) {
        modifiers.push('vpad-xl-double');
    }

    if (props.vpadXlTriple) {
        modifiers.push('vpad-xl-triple');
    }

    if (props.vpadXlQuadruple) {
        modifiers.push('vpad-xl-quadruple');
    }

    if (props.tpadNone) {
        modifiers.push('tpad-none');
    }

    if (props.tpadHalf) {
        modifiers.push('tpad-half');
    }

    if (props.tpadDefault) {
        modifiers.push('tpad-default');
    }

    if (props.tpadDouble) {
        modifiers.push('tpad-double');
    }

    if (props.tpadTriple) {
        modifiers.push('tpad-triple');
    }

    if (props.tpadQuadruple) {
        modifiers.push('tpad-quadruple');
    }

    if (props.tpadXsNone) {
        modifiers.push('tpad-xs-none');
    }

    if (props.tpadXsHalf) {
        modifiers.push('tpad-xs-half');
    }

    if (props.tpadXsDefault) {
        modifiers.push('tpad-xs-default');
    }

    if (props.tpadXsDouble) {
        modifiers.push('tpad-xs-double');
    }

    if (props.tpadXsTriple) {
        modifiers.push('tpad-xs-triple');
    }

    if (props.tpadXsQuadruple) {
        modifiers.push('tpad-xs-quadruple');
    }

    if (props.tpadSmNone) {
        modifiers.push('tpad-sm-none');
    }

    if (props.tpadSmHalf) {
        modifiers.push('tpad-sm-half');
    }

    if (props.tpadSmDefault) {
        modifiers.push('tpad-sm-default');
    }

    if (props.tpadSmDouble) {
        modifiers.push('tpad-sm-double');
    }

    if (props.tpadSmTriple) {
        modifiers.push('tpad-sm-triple');
    }

    if (props.tpadSmQuadruple) {
        modifiers.push('tpad-sm-quadruple');
    }

    if (props.tpadMdNone) {
        modifiers.push('tpad-md-none');
    }

    if (props.tpadMdHalf) {
        modifiers.push('tpad-md-half');
    }

    if (props.tpadMdDefault) {
        modifiers.push('tpad-md-default');
    }

    if (props.tpadMdDouble) {
        modifiers.push('tpad-md-double');
    }

    if (props.tpadMdTriple) {
        modifiers.push('tpad-md-triple');
    }

    if (props.tpadMdQuadruple) {
        modifiers.push('tpad-md-quadruple');
    }

    if (props.tpadLgNone) {
        modifiers.push('tpad-lg-none');
    }

    if (props.tpadLgHalf) {
        modifiers.push('tpad-lg-half');
    }

    if (props.tpadLgDefault) {
        modifiers.push('tpad-lg-default');
    }

    if (props.tpadLgDouble) {
        modifiers.push('tpad-lg-double');
    }

    if (props.tpadLgTriple) {
        modifiers.push('tpad-lg-triple');
    }

    if (props.tpadLgQuadruple) {
        modifiers.push('tpad-lg-quadruple');
    }

    if (props.tpadXlNone) {
        modifiers.push('tpad-xl-none');
    }

    if (props.tpadXlHalf) {
        modifiers.push('tpad-xl-half');
    }

    if (props.tpadXlDefault) {
        modifiers.push('tpad-xl-default');
    }

    if (props.tpadXlDouble) {
        modifiers.push('tpad-xl-double');
    }

    if (props.tpadXlTriple) {
        modifiers.push('tpad-xl-triple');
    }

    if (props.tpadXlQuadruple) {
        modifiers.push('tpad-xl-quadruple');
    }

    if (props.bpadNone) {
        modifiers.push('bpad-none');
    }

    if (props.bpadHalf) {
        modifiers.push('bpad-half');
    }

    if (props.bpadDefault) {
        modifiers.push('bpad-default');
    }

    if (props.bpadDouble) {
        modifiers.push('bpad-double');
    }

    if (props.bpadTriple) {
        modifiers.push('bpad-triple');
    }

    if (props.bpadQuadruple) {
        modifiers.push('bpad-quadruple');
    }

    if (props.bpadXsNone) {
        modifiers.push('bpad-xs-none');
    }

    if (props.bpadXsHalf) {
        modifiers.push('bpad-xs-half');
    }

    if (props.bpadXsDefault) {
        modifiers.push('bpad-xs-default');
    }

    if (props.bpadXsDouble) {
        modifiers.push('bpad-xs-double');
    }

    if (props.bpadXsTriple) {
        modifiers.push('bpad-xs-triple');
    }

    if (props.bpadXsQuadruple) {
        modifiers.push('bpad-xs-quadruple');
    }

    if (props.bpadSmNone) {
        modifiers.push('bpad-sm-none');
    }

    if (props.bpadSmHalf) {
        modifiers.push('bpad-sm-half');
    }

    if (props.bpadSmDefault) {
        modifiers.push('bpad-sm-default');
    }

    if (props.bpadSmDouble) {
        modifiers.push('bpad-sm-double');
    }

    if (props.bpadSmTriple) {
        modifiers.push('bpad-sm-triple');
    }

    if (props.bpadSmQuadruple) {
        modifiers.push('bpad-sm-quadruple');
    }

    if (props.bpadMdNone) {
        modifiers.push('bpad-md-none');
    }

    if (props.bpadMdHalf) {
        modifiers.push('bpad-md-half');
    }

    if (props.bpadMdDefault) {
        modifiers.push('bpad-md-default');
    }

    if (props.bpadMdDouble) {
        modifiers.push('bpad-md-double');
    }

    if (props.bpadMdTriple) {
        modifiers.push('bpad-md-triple');
    }

    if (props.bpadMdQuadruple) {
        modifiers.push('bpad-md-quadruple');
    }

    if (props.bpadLgNone) {
        modifiers.push('bpad-lg-none');
    }

    if (props.bpadLgHalf) {
        modifiers.push('bpad-lg-half');
    }

    if (props.bpadLgDefault) {
        modifiers.push('bpad-lg-default');
    }

    if (props.bpadLgDouble) {
        modifiers.push('bpad-lg-double');
    }

    if (props.bpadLgTriple) {
        modifiers.push('bpad-lg-triple');
    }

    if (props.bpadLgQuadruple) {
        modifiers.push('bpad-lg-quadruple');
    }

    if (props.bpadXlNone) {
        modifiers.push('bpad-xl-none');
    }

    if (props.bpadXlHalf) {
        modifiers.push('bpad-xl-half');
    }

    if (props.bpadXlDefault) {
        modifiers.push('bpad-xl-default');
    }

    if (props.bpadXlDouble) {
        modifiers.push('bpad-xl-double');
    }

    if (props.bpadXlTriple) {
        modifiers.push('bpad-xl-triple');
    }

    if (props.bpadXlQuadruple) {
        modifiers.push('bpad-xl-quadruple');
    }

    return modifiers;
}