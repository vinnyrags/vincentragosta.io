export function maybeAddPaddingModifiers(props) {
    let modifiers = [];

    if (props.padNone) {
        modifiers.push('pad-none');
    }

    if (props.padHalf) {
        modifiers.push('pad-half');
    }

    if (props.padDefault) {
        modifiers.push('pad-default');
    }

    if (props.padDouble) {
        modifiers.push('pad-double');
    }

    if (props.padTriple) {
        modifiers.push('pad-triple');
    }

    if (props.padQuadruple) {
        modifiers.push('pad-quadruple');
    }

    if (props.padXsNone) {
        modifiers.push('pad-xs-none');
    }

    if (props.padXsHalf) {
        modifiers.push('pad-xs-half');
    }

    if (props.padXsDefault) {
        modifiers.push('pad-xs-default');
    }

    if (props.padXsDouble) {
        modifiers.push('pad-xs-double');
    }

    if (props.padXsTriple) {
        modifiers.push('pad-xs-triple');
    }

    if (props.padXsQuadruple) {
        modifiers.push('pad-xs-quadruple');
    }

    if (props.padSmNone) {
        modifiers.push('pad-sm-none');
    }

    if (props.padSmHalf) {
        modifiers.push('pad-sm-half');
    }

    if (props.padSmDefault) {
        modifiers.push('pad-sm-default');
    }

    if (props.padSmDouble) {
        modifiers.push('pad-sm-double');
    }

    if (props.padSmTriple) {
        modifiers.push('pad-sm-triple');
    }

    if (props.padSmQuadruple) {
        modifiers.push('pad-sm-quadruple');
    }

    if (props.padMdNone) {
        modifiers.push('pad-md-none');
    }

    if (props.padMdHalf) {
        modifiers.push('pad-md-half');
    }

    if (props.padMdDefault) {
        modifiers.push('pad-md-default');
    }

    if (props.padMdDouble) {
        modifiers.push('pad-md-double');
    }

    if (props.padMdTriple) {
        modifiers.push('pad-md-triple');
    }

    if (props.padMdQuadruple) {
        modifiers.push('pad-md-quadruple');
    }

    if (props.padLgNone) {
        modifiers.push('pad-lg-none');
    }

    if (props.padLgHalf) {
        modifiers.push('pad-lg-half');
    }

    if (props.padLgDefault) {
        modifiers.push('pad-lg-default');
    }

    if (props.padLgDouble) {
        modifiers.push('pad-lg-double');
    }

    if (props.padLgTriple) {
        modifiers.push('pad-lg-triple');
    }

    if (props.padLgQuadruple) {
        modifiers.push('pad-lg-quadruple');
    }

    if (props.padXlNone) {
        modifiers.push('pad-xl-none');
    }

    if (props.padXlHalf) {
        modifiers.push('pad-xl-half');
    }

    if (props.padXlDefault) {
        modifiers.push('pad-xl-default');
    }

    if (props.padXlDouble) {
        modifiers.push('pad-xl-double');
    }

    if (props.padXlTriple) {
        modifiers.push('pad-xl-triple');
    }

    if (props.padXlQuadruple) {
        modifiers.push('pad-xl-quadruple');
    }

    return modifiers;
}