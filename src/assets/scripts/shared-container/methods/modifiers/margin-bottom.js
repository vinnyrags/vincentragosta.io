export function maybeAddMarginBottomModifiers(props) {
    let modifiers = [];

    if (props.bmarginNone) {
        modifiers.push('bmargin-none');
    }

    if (props.bmarginHalf) {
        modifiers.push('bmargin-half');
    }

    if (props.bmarginDefault) {
        modifiers.push('bmargin-default');
    }

    if (props.bmarginDouble) {
        modifiers.push('bmargin-double');
    }

    if (props.bmarginTriple) {
        modifiers.push('bmargin-triple');
    }

    if (props.bmarginQuadruple) {
        modifiers.push('bmargin-quadruple');
    }

    if (props.bmarginXsNone) {
        modifiers.push('bmargin-xs-none');
    }

    if (props.bmarginXsHalf) {
        modifiers.push('bmargin-xs-half');
    }

    if (props.bmarginXsDefault) {
        modifiers.push('bmargin-xs-default');
    }

    if (props.bmarginXsDouble) {
        modifiers.push('bmargin-xs-double');
    }

    if (props.bmarginXsTriple) {
        modifiers.push('bmargin-xs-triple');
    }

    if (props.bmarginXsQuadruple) {
        modifiers.push('bmargin-xs-quadruple');
    }

    if (props.bmarginSmNone) {
        modifiers.push('bmargin-sm-none');
    }

    if (props.bmarginSmHalf) {
        modifiers.push('bmargin-sm-half');
    }

    if (props.bmarginSmDefault) {
        modifiers.push('bmargin-sm-default');
    }

    if (props.bmarginSmDouble) {
        modifiers.push('bmargin-sm-double');
    }

    if (props.bmarginSmTriple) {
        modifiers.push('bmargin-sm-triple');
    }

    if (props.bmarginSmQuadruple) {
        modifiers.push('bmargin-sm-quadruple');
    }

    if (props.bmarginMdNone) {
        modifiers.push('bmargin-md-none');
    }

    if (props.bmarginMdHalf) {
        modifiers.push('bmargin-md-half');
    }

    if (props.bmarginMdDefault) {
        modifiers.push('bmargin-md-default');
    }

    if (props.bmarginMdDouble) {
        modifiers.push('bmargin-md-double');
    }

    if (props.bmarginMdTriple) {
        modifiers.push('bmargin-md-triple');
    }

    if (props.bmarginMdQuadruple) {
        modifiers.push('bmargin-md-quadruple');
    }

    if (props.bmarginLgNone) {
        modifiers.push('bmargin-lg-none');
    }

    if (props.bmarginLgHalf) {
        modifiers.push('bmargin-lg-half');
    }

    if (props.bmarginLgDefault) {
        modifiers.push('bmargin-lg-default');
    }

    if (props.bmarginLgDouble) {
        modifiers.push('bmargin-lg-double');
    }

    if (props.bmarginLgTriple) {
        modifiers.push('bmargin-lg-triple');
    }

    if (props.bmarginLgQuadruple) {
        modifiers.push('bmargin-lg-quadruple');
    }

    if (props.bmarginXlNone) {
        modifiers.push('bmargin-xl-none');
    }

    if (props.bmarginXlHalf) {
        modifiers.push('bmargin-xl-half');
    }

    if (props.bmarginXlDefault) {
        modifiers.push('bmargin-xl-default');
    }

    if (props.bmarginXlDouble) {
        modifiers.push('bmargin-xl-double');
    }

    if (props.bmarginXlTriple) {
        modifiers.push('bmargin-xl-triple');
    }

    if (props.bmarginXlQuadruple) {
        modifiers.push('bmargin-xl-quadruple');
    }

    return modifiers;
}