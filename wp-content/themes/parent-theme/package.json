{
  "name": "parent-theme",
  "version": "1.0.0",
  "description": "A foundational WordPress parent theme providing PHP infrastructure and base frontend assets",
  "scripts": {
    "compile-providers": "node scripts/compile-providers.js",
    "watch-providers": "node scripts/compile-providers.js --watch",
    "build": "npm run compile-providers",
    "start": "npm run watch-providers"
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "sass": "^1.86.0"
  }
}
