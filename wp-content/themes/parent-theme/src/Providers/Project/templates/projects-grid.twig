<div class="projects-grid">
    {% for project in projects %}
        {% include 'project-card.twig' with { project: project } only %}
    {% else %}
        <p>No projects to display.</p>
    {% endfor %}
</div>
