<div class="projects-grid">
    {% for project in projects %}
        {% include 'blocks/projects/templates/card.twig' with { post: project } %}
    {% else %}
        <p>No projects to display.</p>
    {% endfor %}
</div>