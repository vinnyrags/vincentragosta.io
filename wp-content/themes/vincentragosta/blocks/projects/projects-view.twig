<div class="projects-grid">
    {% for project in projects %}
        {% include ['tease-project.twig', 'tease.twig'] with { post: project } %}
    {% else %}
        <p>No projects to display.</p>
    {% endfor %}
</div>